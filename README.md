# Images-Search-Filter
Creating an interactive image search filter involves a combination of HTML, CSS, and JavaScript. The basic structure uses HTML to define a search input field and containers for the images, with CSS styling to control their layout and appearance. JavaScript powers the functionality: an event listener attached to the search bar captures 
